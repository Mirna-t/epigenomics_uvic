# labCol=NA,
labRow=NA,
density.info="none",
hclust=function(x) hclust(x,method="complete"),
distfun=function(x) as.dist((1-cor(t(x)))/2),srtCol=45
)
cols <- colorRampPalette(brewer.pal(10, "RdBu"))(256)
heatmap.2x(as.matrix(data), col=rev(cols), Colv = T,Rowv=TRUE, scale="none",
ColSideColors=spcol,
trace="none",
dendrogram="column",
cexRow=1, cexCol=.7,
main="WT vs KO Proms + Islands",
# labCol=NA,
labRow=NA,
density.info="none",
hclust=function(x) hclust(x,method="complete"),
distfun=function(x) as.dist((1-cor(t(x)))/2),srtCol=45
)
dim(my.data)
heatmap.2x(as.matrix(my.data), col=rev(cols), Colv = T,Rowv=TRUE, scale="none",
ColSideColors=spcol,
trace="none",
dendrogram="column",
cexRow=1, cexCol=.7,
main="WT vs KO Proms + Islands",
# labCol=NA,
labRow=NA,
density.info="none",
hclust=function(x) hclust(x,method="complete"),
distfun=function(x) as.dist((1-cor(t(x)))/2),srtCol=45
)
data <- t(scale(t(my.data))) # z-score normalise each row (feature)
head(data)
range(data)
data <- t(scale(t(my.data))) # z-score normalise each row (feature)
data <- apply(data, 2, function(x) ifelse(x > 4, 4, x)) # compress data within range
data <- apply(data, 2, function(x) ifelse(x < -4, -4, x)) # compress data within range
spcol<-samples
cols <- colorRampPalette(brewer.pal(10, "RdBu"))(256)
heatmap.2x(as.matrix(my.data), col=rev(cols), Colv = T,Rowv=TRUE, scale="none",
ColSideColors=spcol,
trace="none",
dendrogram="column",
cexRow=1, cexCol=.7,
main="WT vs KO Proms + Islands",
# labCol=NA,
labRow=NA,
density.info="none",
hclust=function(x) hclust(x,method="complete"),
distfun=function(x) as.dist((1-cor(t(x)))/2),srtCol=45
)
heatmap.2x(as.matrix(data), col=rev(cols), Colv = T,Rowv=TRUE, scale="none",
ColSideColors=spcol,
trace="none",
dendrogram="column",
cexRow=1, cexCol=.7,
main="WT vs KO Proms + Islands",
# labCol=NA,
labRow=NA,
density.info="none",
hclust=function(x) hclust(x,method="complete"),
distfun=function(x) as.dist((1-cor(t(x)))/2),srtCol=45
)
library(clusterProfiler)
library(org.Hs.eg.db)
#library(GOstats)
library(GO.db)
dim(proms.island.filt)
genesid<-proms.island.filt$UCSC_RefGene_Name
genesid
genesid<- strsplit(as.character(genesid),';')
genesid
genesid<-unique(unlist(genesid))
genesid
genesid<-genesid[ genesid != "" ]##remove empty elements from a vector
genesid <- genesid[!is.na(genesid)]
genesid
eg<-bitr(genesid, fromType="SYMBOL", toType="ENTREZID",
OrgDb="org.Hs.eg.db")
?enrichGO
ego2 <- enrichGO(gene         = eg$ENTREZID,
OrgDb         = org.Hs.eg.db,
keyType       = 'ENTREZID',
ont           = "BP",
pAdjustMethod = "BH",
pvalueCutoff  = 0.05,
qvalueCutoff  = 0.05,
readable=T
)
dotplot(ego2, title=" Prom islands Cpgs ",showCategory=20)
dotplot(ego2, title=" Prom islands Cpgs ",showCategory=10)
dotplot(ego2,
title=" Prom islands Cpgs ",
showCategory=15)
dotplot(ego2,
title=" Prom islands Cpgs ",
showCategory=20)
dim(ego2@result)
head(ego2@result)
head(ego2@result)
dotplot(ego2,
title=" Prom islands Cpgs ",
showCategory=20)
ego2@result %>%
dplyr::filter(grepl("carbon",Description))
ego2@result %>%
dplyr::filter(grepl("metabolism",Description))
ego2@result %>%
dplyr::filter(grepl("metabolism| mito| nucleotide",Description))
ego2@result %>%
dplyr::filter(grepl("metabolism| mito| nucleotide",Description))
ego2@result %>%
dplyr::filter(grepl("metabolism| mito| nucleotide",Description)) %>% pull(Description)
ego2@result %>%
dplyr::filter(grepl("metabolism| mito| nucleotide",Description)) %>% select(Description,qvalue)
ego2@result
ego2@result %>%
dplyr::filter(grepl("metabolism| mito| nucleotide",Description)) %>% dplyr::select(Description,qvalue)
UP<- proms.island.filt  %>%
dplyr::filter(logFC>0)
dim(UP)
genesid<-proms.island.filt$UCSC_RefGene_Name
genesid<- strsplit(as.character(genesid),';')
genesid<-unique(unlist(genesid))
genesid<-genesid[ genesid != "" ]##remove empty elements from a vector
genesid <- genesid[!is.na(genesid)]
eg<-bitr(genesid, fromType="SYMBOL", toType="ENTREZID",
OrgDb="org.Hs.eg.db")
ego2 <- enrichGO(gene         = eg$ENTREZID,
OrgDb         = org.Hs.eg.db,
keyType       = 'ENTREZID',
ont           = "BP",
pAdjustMethod = "BH",
pvalueCutoff  = 0.05,
qvalueCutoff  = 0.05,
readable=T
)
dotplot(ego2,
title=" Prom islands Cpgs ",
showCategory=20)
ego2@result %>%
dplyr::filter(grepl("carbon",Description))
ego2@result %>%
dplyr::filter(grepl("metabolism| mito| nucleotide",Description)) %>% dplyr::select(Description,qvalue)
DOWN<- proms.island.filt  %>%
dplyr::filter(logFC<0)
genesid<-DOWN$UCSC_RefGene_Name
genesid<- strsplit(as.character(genesid),';')
genesid<-unique(unlist(genesid))
genesid<-genesid[ genesid != "" ]##remove empty elements from a vector
genesid <- genesid[!is.na(genesid)]
eg<-bitr(genesid, fromType="SYMBOL", toType="ENTREZID",
OrgDb="org.Hs.eg.db")
ego2 <- enrichGO(gene         = eg$ENTREZID,
OrgDb         = org.Hs.eg.db,
keyType       = 'ENTREZID',
ont           = "BP",
pAdjustMethod = "BH",
pvalueCutoff  = 0.05,
qvalueCutoff  = 0.05,
readable=T
)
dotplot(ego2,
title=" Prom islands Cpgs ",
showCategory=20)
pData(gRatioSet.illumina)$Status
design <- model.matrix(~pData(gRatioSet.illumina)$Status)##
design
head(design)
group<-as.factor(pData(gRatioSet.quantile)$Status)
group
group<-relevel(group,ref="WT")
group
design <- model.matrix(~group)##
head(design)
group<-as.factor(pData(gRatioSet.illumina)$Status)
group<-relevel(group,ref="WT")
group
design <- model.matrix(~group)##
head(design)
group <- as.factor(pData(gRatioSet.illumina)$Status)
group
group<-relevel(group,ref="WT")
group
designMatrix <- model.matrix(~ group)
head(designMatrix)
dim(Mval)
head(designMatrix)
myannotation <- cpg.annotate("array",
Mval,
what="M",
arraytype = "EPIC",
analysis.type="differential",
design=designMatrix,
coef=2)
designMatrix
myannotation <- cpg.annotate("array",
Mval,
what="M",
arraytype = "EPIC",
analysis.type="differential",
design=designMatrix,
coef=2)
?cpg.annotate
myannotation <- cpg.annotate("array",
betas,
what="Beta",
arraytype = "EPIC",
analysis.type="differential",
design=designMatrix,
coef=2)
myannotation
myannotation <- cpg.annotate("array",
Mval[1:1000,],
what="M",
arraytype = "EPIC",
analysis.type="differential",
design=designMatrix,
coef=2)
Mval[1:1000,]
myannotation <- cpg.annotate("array",
Mval[1:500,],
what="M",
arraytype = "EPIC",
analysis.type="differential",
design=designMatrix,
coef=2)
myannotation
rm(list=ls())
setwd("/Users/agomez/Downloads/GSE304711_RAW/")
idat.folder <- "/Users/agomez/Downloads/GSE304711_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
rgset <- read.metharray.exp(targets = targets,verbose = T)
getwd()
setwd("/Users/agomez/Downloads/GSE304711_RAW/")
idat.folder <- "/Users/agomez/Downloads/GSE304711_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
rgset <- read.metharray.exp(targets = targets,verbose = T)
rgset <- read.metharray.exp(targets = targets,verbose = T,force=TRUE)
rgset <- read.metharray.exp(targets = targets,verbose = T,force=TRUE)
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
setwd("/Users/agomez/Downloads/GSE304711_RAW/")
idat.folder <- "/Users/agomez/Downloads/GSE304711_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
rm(list=ls)
rm(list=ls())
gc()
setwd("/Users/agomez/Downloads/GSE304711_RAW/")
idat.folder <- "/Users/agomez/Downloads/GSE304711_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
setwd("/Users/agomez/Desktop/GSE304711_RAW/")
idat.folder <- "/Users/agomez/Desktop/GSE304711_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
setwd("/Users/agomez/Desktop/GSE304711_RAW/")
idat.folder <- "/Users/agomez/Desktop/GSE304711_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
rgset <- read.metharray.exp(targets = targets,verbose = T)
rgset
phenoData <- pData(rgset)
MSet <- preprocessRaw(rgset)
rgSet@annotation=c(array="IlluminaHumanMethylationEPIC",annotation="ilm10b2.hg19")
rgset@annotation=c(array="IlluminaHumanMethylationEPIC",annotation="ilm10b2.hg19")
rgset
phenoData <- pData(rgset)
MSet <- preprocessRaw(rgset)
#The functions getQC and plotQC are designed to extract and plot the quality control information from the MethylSet:
qc <- getQC(preprocessRaw(rgset) )
plotQC(qc)
setwd("/Users/agomez/Downloads/GSE247810_RAW/")
idat.folder <- "/Users/agomez/Downloads/GSE247810_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
rgset <- read.metharray.exp(targets = targets,verbose = T)
phenoData <- pData(rgset)
phenoData
MSet <- preprocessRaw(rgset)
#The functions getQC and plotQC are designed to extract and plot the quality control information from the MethylSet:
qc <- getQC(preprocessRaw(rgset) )
plotQC(qc)
phenoData
#To further explore the quality of the samples, it is useful to look at the Beta value densities of the samples, with the option to color the densities by group:
densityPlot(preprocessRaw(rgset) ,
sampGroups = pData(rgset)$Treatment)
detP<-detectionP(rgset)
barplot(colMeans(detP),col=factor(pData(rgset)$Status),
las=2,cex.names=0.8,main="Mean detection p-values",
ylim = c(0,0.01)
)
barplot(colMeans(detP),col=factor(pData(rgset)$Treatment),
las=2,cex.names=0.8,main="Mean detection p-values",
ylim = c(0,0.01)
)
keep<-colMeans(detP) < 0.01
table(keep)
rgset<-rgset[,keep]
##already
targets<-pData(rgset)
keep_probes <- rowSums(detP < 0.01) == ncol(rgset)
table(keep_probes)
rgset <- rgset[keep_probes,]
gRatioSet.illumina <- preprocessIllumina(rgset)##Illumina
ann850k<-getAnnotation(IlluminaHumanMethylationEPICanno.ilm10b4.hg19)
ann850k<-as.data.frame(ann850k)
###M values for analysis
Mval<-getM(gRatioSet.illumina)
Mval <- rmSNPandCH(Mval, dist=2,
mafcut=0.05,
rmcrosshyb = T,
rmXY = T) ### get out SNPs of my samples
betas <- rmSNPandCH(betas, dist=2,
mafcut=0.05,
rmcrosshyb = T,
rmXY = T) ### get out SNPs of my samples
betas<-getBeta(gRatioSet.illumina)
betas <- rmSNPandCH(betas, dist=2,
mafcut=0.05,
rmcrosshyb = T,
rmXY = T) ### get out SNPs of my samples
library(FactoMineR)
library(ggplot2)
pca <- FactoMineR::PCA(t(na.omit(as.matrix(betas))),
scale.unit = T, graph = F, ncp = 20)
factoextra::fviz_pca_ind(pca, axes = c(1,2),
habillage=as.factor(targets$Treatment), repel = F,
label = "") +
coord_fixed()
group<-as.factor(pData(gRatioSet.illumina)$Treatment)
pData(gRatioSet.illumina)$Treatment
group<-as.factor(pData(gRatioSet.illumina)$Treatment)
group<-relevel(group,ref="DMSO")
group
design <- model.matrix(~group)##
colnames(design)[2]<-"Comp"
####generate adjusted beta matrix
fit <- lmFit(Mval, design)
fit2 <- eBayes(fit)
results <- topTable(fit2,
coef="Comp",
num=dim(fit2)[1],
sort.by="P",
adjust.method = "BH") ####Pval list 1
res<-subset(results,results$adj.P.Val<.05)
dim(res)
head(results)
res<-subset(results,results$P.Value<.05)
dim(res)
idat.folder <- "/Users/agomez/Downloads/GSE247810_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
setwd("/Users/agomez/Downloads/GSE247810_RAW/")
idat.folder <- "/Users/agomez/Downloads/GSE247810_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
setwd("/Users/agomez/Downloads/GSE247810_RAW/")
idat.folder <- "/Users/agomez/Downloads/GSE247810_RAW/" ###change for wour path dir
targets <- read.metharray.sheet(base=idat.folder,verbose = T)
targets
factoextra::fviz_pca_ind(pca, axes = c(1,2),
habillage=as.factor(targets$Tumor_Line), repel = F,
label = "") +
coord_fixed()
design <- model.matrix(~Treatment + Tumor_Line,data = targets)##
colnames(design)[2]<-"Comp"
head(design)
####generate adjusted beta matrix
fit <- lmFit(Mval, design)
fit2 <- eBayes(fit)
results <- topTable(fit2,
coef="Comp",
num=dim(fit2)[1],
sort.by="P",
adjust.method = "BH") ####Pval list 1
head(results)
res<-subset(results,results$adj.P.Val<.05)
dim(res)
head(res)
##base R
res <- merge(res,
ann850k[,c("UCSC_RefGene_Name","Relation_to_Island","UCSC_RefGene_Group")],
by="row.names")
dim(res)
hist(res$logFC)
proms<-res[grep("TSS1500|TSS200|5'UTR|1stExon",res$UCSC_RefGene_Group),]
proms.island<-proms[proms$Relation_to_Island=="Island",]
dim(proms.island)
head(proms.island)
##subset data
my.data<-betas[rownames(betas) %in% proms.island$Row.names,]
samples<-ifelse(targets$Treatment=="JQ1","red","blue1")
samples
data <- t(scale(t(my.data))) # z-score normalise each row (feature)
data <- apply(data, 2, function(x) ifelse(x > 4, 4, x)) # compress data within range
data <- apply(data, 2, function(x) ifelse(x < -4, -4, x)) # compress data within range
spcol<-samples
cols <- colorRampPalette(brewer.pal(10, "RdBu"))(256)
heatmap.2x(as.matrix(data), col=rev(cols), Colv = T,Rowv=TRUE, scale="none",
ColSideColors=spcol,
trace="none",
dendrogram="column",
cexRow=1, cexCol=.7,
main="WT vs KO Proms + Islands",
# labCol=NA,
labRow=NA,
density.info="none",
hclust=function(x) hclust(x,method="complete"),
distfun=function(x) as.dist((1-cor(t(x)))/2),srtCol=45
)
dim(data)
head(data)
spcol
cols <- colorRampPalette(brewer.pal(10, "RdBu"))(256)
heatmap.2x(as.matrix(data), col=rev(cols), Colv = T,Rowv=TRUE, scale="none",
ColSideColors=spcol,
trace="none",
dendrogram="column",
cexRow=1, cexCol=.7,
main="WT vs KO Proms + Islands",
# labCol=NA,
labRow=NA,
density.info="none",
hclust=function(x) hclust(x,method="complete"),
distfun=function(x) as.dist((1-cor(t(x)))/2),srtCol=45
)
genesid<-proms.island$UCSC_RefGene_Name
genesid<- strsplit(as.character(genesid),';')
genesid<-unique(unlist(genesid))
genesid<-genesid[ genesid != "" ]##remove empty elements from a vector
genesid <- genesid[!is.na(genesid)]
eg<-bitr(genesid, fromType="SYMBOL", toType="ENTREZID",
OrgDb="org.Hs.eg.db")
ego2 <- enrichGO(gene         = eg$ENTREZID,
OrgDb         = org.Hs.eg.db,
keyType       = 'ENTREZID',
ont           = "BP",
pAdjustMethod = "BH",
pvalueCutoff  = 0.05,
qvalueCutoff  = 0.05,
readable=T
)
dotplot(ego2,
title=" Prom islands Cpgs ",
showCategory=20)
my.data<- betas %>%
as.data.frame() %>%
dplyr::filter(rownames(.) %in% proms.island$Row.names)
dim(my.data)
head(my.data)
data <- t(scale(t(my.data))) # z-score normalise each row (feature)
data <- apply(data, 2, function(x) ifelse(x > 4, 4, x)) # compress data within range
data <- apply(data, 2, function(x) ifelse(x < -4, -4, x)) # compress data within range
spcol<-samples
cols <- colorRampPalette(brewer.pal(10, "RdBu"))(256)
heatmap.2x(as.matrix(data), col=rev(cols), Colv = T,Rowv=TRUE, scale="none",
ColSideColors=spcol,
trace="none",
dendrogram="column",
cexRow=1, cexCol=.7,
main="WT vs KO Proms + Islands",
# labCol=NA,
labRow=NA,
density.info="none",
hclust=function(x) hclust(x,method="complete"),
distfun=function(x) as.dist((1-cor(t(x)))/2),srtCol=45
)
table(is.na(data))
data
data<- data[complete.cases(data), ]
heatmap.2x(as.matrix(data), col=rev(cols), Colv = T,Rowv=TRUE, scale="none",
ColSideColors=spcol,
trace="none",
dendrogram="column",
cexRow=1, cexCol=.7,
main="WT vs KO Proms + Islands",
# labCol=NA,
labRow=NA,
density.info="none",
hclust=function(x) hclust(x,method="complete"),
distfun=function(x) as.dist((1-cor(t(x)))/2),srtCol=45
)
UP<- proms.island  %>%
dplyr::filter(logFC>0)
DOWN<- proms.island  %>%
dplyr::filter(logFC<0)
genesid<-UP$UCSC_RefGene_Name
genesid<- strsplit(as.character(genesid),';')
genesid<-unique(unlist(genesid))
genesid<-genesid[ genesid != "" ]##remove empty elements from a vector
genesid <- genesid[!is.na(genesid)]
eg<-bitr(genesid, fromType="SYMBOL", toType="ENTREZID",
OrgDb="org.Hs.eg.db")
ego2 <- enrichGO(gene         = eg$ENTREZID,
OrgDb         = org.Hs.eg.db,
keyType       = 'ENTREZID',
ont           = "BP",
pAdjustMethod = "BH",
pvalueCutoff  = 0.05,
qvalueCutoff  = 0.05,
readable=T
)
do tplot(ego2,
dotplot(ego2,
title=" Prom islands Cpgs ",
showCategory=20)
ego2@result %>% dplyr::filter(p.adjust < 0.05)
ego2@result %>% dplyr::filter(p.adjust < 0.05) %>% pull(Description)
